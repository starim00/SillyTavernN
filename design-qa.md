# 前端设计检查

依据 [产品与设计准则](docs/DESIGN_PRINCIPLES.md) 检查实际渲染结果。以下是验收要求，不是某次运行的通过报告。

## 布局与阅读

- 卡片选择、该卡聊天记录和新建对话入口符合卡片优先的导航。
- 消息自然展开，消息流占主要空间；辅助面板能够收起。
- 使用现有颜色 token、字体和 Phosphor 图标，主要操作、选中态与状态提示区分清楚。
- 输入区紧凑并随草稿增长；脚本按钮位于原生输入区下方。
- 桌面和窄屏都检查长内容、弹窗、面板、安全区域和实际滚动行为。

## 编辑与破坏性操作

- 变量树覆盖深层对象、数组、长键名、各 JSON 类型，以及节点保存与取消；原始 JSON 批量编辑保持可用。
- 世界书和预设编辑器不横向溢出，按钮与类型选择器不被裁切。
- 删除消息的确认范围包含当前及后续楼层，取消不改变数据，确认后刷新仍一致。
- 工具提案、权限拒绝和 revision 冲突在界面有可辨认的结果。

## 证据与检查范围

普通 UI 检查使用 Codex 内置 Browser；依赖现有登录、标签页或扩展状态时使用连接的 Chrome。独立 Playwright 的适用条件见根目录 [AGENTS.md](AGENTS.md)。

记录本次实际检查的视口、数据状态、操作和结果。组件测试用于验证交互合同，浏览器检查用于验证布局和完整流程；模拟窄屏不等于真机测试。纯文档修改通常只需检查事实、链接和格式，无需启动浏览器。
